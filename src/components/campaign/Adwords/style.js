import csjs from 'csjs'

export const style = csjs`
.edit {
  margin-left: 1em;
  font-size: x-small;
  cursor: pointer;
  text-transform: lowercase;
}
.subText {
  font-size: .6em;
  font-weight: 400;
  margin-left: 1em;
}
.title {
  margin: .3em 0;
  overflow: hidden;
}
.extensions {
  margin-left: 2em;
  margin-bottom: 1em;
}
.extTitle extends .title {
  margin-top: 1em;
}`
